*.pyc
*.iml
*.swp
.project
.pydevproject
.DS_Store
.idea

.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# colcon/CMake output
build
install
log

# local CI / lint venv (not part of lunar's uv env)
.ci-venv/

src/sensor/build

rosbag*

# Native firmware unit test binary (built by firmware/arduino/tests/Makefile)
firmware/arduino/tests/test_encoder_quadrature_runner

# Local arena / field samples (keep out of git unless you commit intentionally)
field_photos/

# Lunar local runtime logs
.lunar/dashboard.log
.lunar/runs/

# Accidental uv workspace lock at repo root (use lunar/uv.lock)
/uv.lock
